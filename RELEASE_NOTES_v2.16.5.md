# ClipSlots v2.16.5

本版继续留在 `2.16.x` 画布线：bug 扫描后的可靠性修复 + 画布撤销能力补齐，不做新功能。

## 画布附件编辑接入撤销 / 重做

- **入参附件的增删改与排序现在可以 Cmd+Z 撤销**：拖入入参文件、删除入参、「设为首个入参」都会产生一条独立历史条目，撤销/重做时附件列表与字节一并归位。
  - 被删附件的字节在写入前复制到磁盘暂存区（`canvas/.attach_stash/`，APFS clonefile 不占额外空间），因此撤销是**字节级还原**，而不只是还原一份附件元数据。
  - 撤销时当前状态独有的附件字节同步移入暂存以支持重做；历史条目被截断 / 切换项目清栈时暂存自动收走，不留孤儿字节。
  - 画布视图不在线时产生的撤销会排队，重新进入画布后补写（沿用文本回滚的同一套机制）。

## 可靠性修复

- **删除项目失败不再产生「孤儿项目」**：项目文档移入 `.trash` 若失败（例如磁盘异常），现在会回滚项目索引并放弃删除。此前索引已无记录、文档仍留在盘上，项目会永久不可见也无法恢复。
- **长连线命中更准**：连线命中判定的采样数从固定 24 段改为按曲线长度自适应（约每 24pt 一段，12~200 段）。8000pt 级长连线在固定采样下弦误差超过 10pt 命中容差，曲线中点会点不中。

## 验证

- `swift build` 通过。
- `swift run ClipSlotsKitSmokeTests`：通过 32257 项，失败 0（较 v2.16.4 新增 21 项，覆盖长距命中、附件编辑历史往返、暂存字节全生命周期）。
- DMG SHA256: `bb75e4b1196d157f5ee1a2676a22853a20c2364d316e306458a5c0310db7e17e`。
