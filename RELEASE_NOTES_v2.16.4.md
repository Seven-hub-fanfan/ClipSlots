# ClipSlots v2.16.4

## 画布交互修复
- **修复长距离双指平移扫过左侧槽位库会突然中断**：滚轮/触控板事件现在在画布消费首帧后锁定本次滚动序列，直到系统结束事件或短暂空闲后再释放。
- 保留侧栏自身滚动语义：如果滚动一开始就在侧栏列表上，仍交给侧栏处理；只有已归属画布的连续手势会保持画布平移。

## TapNow 对比优化
- 交互口径更接近 TapNow / Figma 这类设计画布：一次平移手势不会因为光标路过浮层或侧栏而改派目标，画布移动更连续。

## 验证
- `swift build` 通过。
- `swift run ClipSlotsKitSmokeTests` 通过 32236 项，失败 0。
- DMG SHA256: `c5bda046a0a6b133986d4903fef14b815d461ad5578dcb56776218ddfb888aad`。
