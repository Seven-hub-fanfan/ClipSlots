# ClipSlots v2.10.95

## ✨ 新功能：Cmd+Z 多步撤销（最近 10 步）

以前只能撤销「最近一次清空/删除」（Ctrl+Z，单步）。现在升级为标准的多步撤销：

- **快捷键**：`Cmd+Z`（旧的 `Ctrl+Z` 作为别名保留）。
- **步数**：每个槽位组各自保留最近 **10 步**，撤销可以连按。
- **清空槽位算一步，撤销后内容全部返回**：快照抓的是整组 10 个槽位的全量状态，
  「清空整组」撤销一次即全部还原，主体内容 + 附件 + 标签一起回来。
- **纳入撤销的操作**：清空单槽 / 清空整组 / 删除槽位内容 / 保存·覆盖槽位（含自动存储落点）/
  编辑文本 / 编辑 HTML / 拖入文件导入 / 修改附件 / 修改标签。
- **附件字节不再断链**：单槽 clear 与清空整组会物理删除槽位目录（含外置附件字节），
  因此抓快照时会把这些槽位的附件字节就地读回快照（单次预算 64MB，超大附件仍可从
  `.trash` 的 30 天克隆里人工恢复），撤销后附件可正常预览/粘贴。
- **跨重启可用**：撤销栈持久化到 `~/.local/share/clipslots/special_slots/.undo/undo_stack.json`，
  重启 App 后仍可继续撤销；旧版单快照文件（`clear_snapshot.json`）会自动迁移一次后删除。
- **不抢文本编辑器的撤销**：焦点在文本框/文本编辑器且它自己有撤销记录时，`Cmd+Z` 交还给
  系统原生文本撤销，不会误撤槽位。
- **不再白占额度**：清空确认弹窗点「取消」不再消耗一步撤销记录（快照移到实际执行处抓）。

## 🛡️ 保持不变的既有不变量

- 撤销只回写到快照被抓取的那个槽位组（v2.8.7 D），绝不污染当前激活的其它组；
  当前组无记录时提示「请切回原分组」。
- 缩略图身份（contentId + updatedAt）契约不变；撤销时整组失效一次缩略图缓存，
  切组不串图、不停留旧图。
- 存储层 STG-2 幽灵组护栏、跨进程写锁、`.trash` 软删除逻辑均未改动。
- 游标胶囊位置与自动存储/自动粘贴行为未改动。

## 🧪 测试

- `swift build` 通过。
- `swift run ClipSlotsKitSmokeTests`：**82 / 82 通过**（新增 23 条断言：撤销栈 10 步上限、
  按组弹出、去重、JSON 跨重启往返，以及「清空 → 快照回写」的主体/附件字节/标签完整还原）。
