# ClipSlots v2.11.7 — 简洁模式皮肤

## 这版做了什么

新增「简洁模式」皮肤，和原来的「多彩模式」并存，在 **设置 → 外观 → 皮肤** 里实时切换，不用重启。

多彩模式的观感一个像素都没动——所有原值被原封不动挪进 `colorful*` 常量，简洁模式只是在读 token 的那一刻分了个叉。

### 简洁模式长什么样

| 元素 | 多彩模式 | 简洁模式 |
| --- | --- | --- |
| 窗口底 | 复古海报氛围层（4 层高斯模糊 + .screen 混合 + 噪点） | 纯净中性底（浅色 `#F2F2F3` / 深色哑光 `#161618`） |
| 卡片 | 米白 / 暖黑 + 顶部槽位色装饰条 | 纯白 `#FFFFFF` / `#232325`，大圆角 24pt，极微弱投影，无装饰条 |
| 槽位编号 | 26pt 彩色裸数字 | **保留槽位色**，收进 30pt 圆形角标——简洁模式唯一的彩色出口 |
| 选中 / 悬停 | 槽位色描边 + 彩色外发光 | 统一紫色细描边（浅色淡紫 `#8B7BE8` / 深色霓虹紫 `#A78BFA`），无外发光 |
| 空槽主按钮 | 彩色按钮 + 下方留白 | 反相大 CTA（浅色近黑底白字 / 深色白底黑字），撑满卡片底部 |
| 编辑 / 清空 | 槽位色 / 暖灰 | 中性灰（浅色 `#EDEDEF` / 深色 `#313133`），低调 |
| 危险操作激活态 | 红 | **仍然是红**（这是二次确认的安全信号，不是装饰色） |

深浅色跟随系统 light / dark，两套皮肤各有独立的浅色与深色取值。

## 实现要点

### 皮肤为什么不能走 dynamic NSColor 那条路

深浅切换之所以能做到「不重算 body、只重绘」，靠的是 `NSColor(name:dynamicProvider:)`：系统 appearance 一变，所有图层自然重绘，provider 顺势解析出新值。

皮肤不一样——它是 App 自己的全局变量，改了它 AppKit 根本不知道，没有任何东西会被标记为「需要重绘」。若只把皮肤塞进 provider 闭包，切换后只有恰好因别的原因重绘的视图会变色，屏幕上会出现一半新皮肤一半旧皮肤。

所以：

- 需要分叉的 token 从 `static let` 改成**计算属性**，分叉发生在「读 token」那一刻；
- `ContentView` 用 `.id(appSkinRaw)` 承接，切皮肤时整棵树重建一次。

代价是切皮肤那一下有一次整树重算，但这是用户一年点两次的显式操作，和「每次切深浅都卡 130ms」不是一个量级的问题。深浅切换依旧走动态色，v2.10.93 那轮优化的成果没有被牺牲。

### 命名

需求里写的是 `AppearanceMode`，但项目里 `appearanceMode` 这个 UserDefaults 键早就被 `ThemeMode`（深色 / 浅色 / 跟随系统）占了。明暗和风格是两个正交维度，硬塞进同一个名字只会在半年后害到自己，所以：

- 枚举叫 `AppSkin`（`.colorful` / `.minimal`）
- 键名叫 `appearanceSkin`
- 默认值 `.colorful`——老用户升级上来不该被换皮肤

smoke 里专门有一条断言盯着这两个键不能重名。

### 改动文件

- `Sources/ClipSlotsKit/AppSkin.swift`（新增）：皮肤枚举 + 持久化 + `MinimalSkinPalette` 中性调色板
- `Sources/ClipSlots/AppSkinCenter.swift`（新增）：运行时皮肤缓存（绘制路径每帧读上百次，不能每次查 UserDefaults）
- `Sources/ClipSlots/AppTheme.swift`：窗口 / 卡片 / 边框 / 投影 / 控件 / 搜索框 / 操作按钮共 15 个 token 按皮肤分叉
- `Sources/ClipSlots/SlotCardView.swift`：装饰条、描边高亮、外发光、编号角标、CTA 按钮、「上次粘贴」角标
- `Sources/ClipSlots/SettingsView.swift`：外观区新增皮肤选择器
- `Sources/ClipSlots/ContentView.swift`：`.id` 重建 + 简洁模式跳过氛围层渲染
- `Sources/ClipSlots/AppDelegate.swift`：外部改 defaults 时同步皮肤缓存

## 验证

- `swift build` 通过
- smoke 测试 **通过 1212，失败 0**（新增 78 条 SKIN 断言）
  - 持久化契约：键名、默认值、脏值回退、与 `appearanceMode` 不重名
  - 中性度：简洁模式全部表面色 RGB 极差 ≤ 0.02（选中紫是唯一例外，且必须 > 0.15）
  - 明暗关系：卡片必须比窗口底亮、空槽比有内容的暗、深色底不是纯黑
  - 对比度：正文 / 次要文字 / 控件 / CTA 全部 ≥ 4.5:1（WCAG AA）
  - 槽位编号在两种简洁卡片底上均 ≥ 3:1（大字 AA）——这是「保留编号颜色」这条需求的真正验收项
- `SKIP_NOTARIZE=1` 打包 + 覆盖安装 `/Applications/ClipSlots.app`，App 与 CLI 版本号均为 2.11.7

## hotfix 1（同版本号重打包）

修一类反向的图标着色问题：装饰性的品牌蓝紫色**在简洁模式里留了下来**，最扎眼的是顶部工具栏的拼图图标（插件入口）——它成了整个简洁界面上唯一的彩色图标。

根因不是「写反了」，而是这些地方**从来没有分皮肤**：它们裸写 `Color.accentColor` / `AppTheme.brandGradient`，两种皮肤下都是彩色；只是多彩模式满屏都是颜色，它混在里面看不出来，简洁模式一收，它就孤零零地跳出来了。

新增一组 `chromeAccent*` token 划清界线：

- **装饰性品牌色** → 多彩模式保留强调色，简洁模式收成中性墨色。共 12 处：
  - 顶部工具栏拼图图标（插件入口）← 用户报的这个
  - App logo 底板（品牌渐变 + 光晕 + 白色图形）
  - 页面选择器的九宫格图标及其底板
  - 「检查更新」胶囊
  - 快捷键提示条底色
  - 「连接」胶囊的**无连接**静息态
  - 卡片里的内容类型图标（文本 / 链接 / 图片…）
  - 附件计数胶囊（有附件时原为品牌渐变，是简洁模式下最大片的彩色残留）
  - 设置侧栏 / 设置页 / 插件市场 / 槽位组管理 四处标题底板
- **功能性强调色** → 两种皮肤都保留：选中的组、启用的拨杆、拖入高亮、已有连接、危险操作红。收成灰会真的丢信息。

由此定下简洁模式的一条规矩：**出现彩色，就意味着「这里有状态」**。槽位编号是唯一的例外（身份标识，本来就该保留）。

smoke 增加 4 条断言，盯住中性色块压在卡片 / 窗口底上不许糊成一片——中性化最容易翻车的地方就是色块和承载面同色、直接消失。

## hotfix 2（同版本号重打包）

上一轮「收编彩色」收得过头，误伤了两处，这轮全部回退成设计意图：

- **卡片顶部的彩色短横线恢复，并且两种皮肤完全一致、不做任何差异化。** 它的颜色跟着槽位编号走，是同一个身份标识在卡片上的第二次表达（一次在角标、一次在顶边），不属于「不携带信息的品牌色」。简洁模式该收的是装饰，不是身份色。
- **简洁模式左上角 Logo 改为展示 App 真实图标**（白底 squircle + 黑色三叠卵石），**56pt**，无底板 / 无描边 / 无投影，与右侧标题垂直居中。此前给它套了中性灰底板，在浅色简洁界面上变成一个「大白圆角按钮」，看着像可点的控件，而它只是个 logo。多彩模式的品牌渐变底板 + 叠层图形保持原样，一点没动。
  - 图标取自运行时 `NSApp.applicationIconImage` 而不是 `Image("AppIcon")`：本项目的图标是 `assets/AppIcon.icns` 在打包时拷进 bundle Resources 的，没有 Assets.xcassets，按名字查资源目录会拿不到；`applicationIconImage` 读的就是当前 bundle 已注册的那份，以后换图标自动跟着变。
  - 尺寸定为 56pt（画布）：`frame` 给的是图标**画布**边长，不是可见图案边长。量过 `assets/AppIcon.png`（1024 画布）——白色 squircle 底板实际只有 824px，即画布的 **80.5%**（余下是 macOS 图标规范的透明安全边 + 投影空间），黑色石头图案更小，只占 474px（46%）。而多彩模式的 logo 是实心铺满的 44×44pt 渐变圆角块。要让简洁模式可见底板不小于它，画布需 44 / 0.805 ≈ 54.7pt，取 56pt。截图实测：多彩 44×44pt、简洁 45×45pt，视觉体量对齐。

`swift build` 通过，smoke **通过 1212，失败 0**；重新打包并覆盖安装，App 与 CLI 版本号仍为 2.11.7。

## hotfix 2d（同版本号重打包）

把**两种皮肤的布局骨架统一**，差异只留颜色和质感：

- **Header logo 共用一个 46×46pt 的盒子**，可见图形都收敛到 ~40pt。多彩模式的实心渐变块从 44pt 收到 40pt（圆角 12→11、图形 20→18pt），简洁模式的图标画布给 50pt（可见白底板 40 / 0.805 ≈ 50）。之前多彩的实心块可见面积明显大于简洁模式的图标，把标题往右顶得更远，header 看着臃肿。现在「ClipSlots」在两种皮肤下的水平起点完全一致。
- **槽位编号统一成 30pt 圆形角标**。多彩模式原来是 26pt 裸黑体数字，墨迹宽度还随位数变化（"1" 和 "10" 差一截），导致卡片标题的水平起点在两种皮肤、甚至不同槽位间都不一致。改成定宽圆形角标后标题起点严格对齐；多彩模式保留更饱和的底色、更实的描边和一层同色投影，分量感不丢。

截图实测两种皮肤的标题起点与卡片首行基线一致。

## hotfix 3（同版本号重打包）：新拟物工具栏 + 自动存储 / 自动粘贴面板

按新设计稿重做了**顶部工具栏**和**自动存储 / 自动粘贴开关面板**两块，风格从「磨砂玻璃 + 金属拨杆」换成**新拟物（neumorphism）**：白 / 浅灰浮起面板、内凹输入槽、微凸按钮，光源统一在左上。其余区域（卡片区、设置、径向菜单）本轮不动。

### 落地了什么

- **工具栏收成一整块浮起面板**：原先 titleBar 和 actionBar 是两条各自带背景的横条，中间夹一条 Divider；现在合成一个大圆角浮起面板，内部两行、中间只留一条 1px 发丝线。
- **搜索框改为内凹槽（well）**：34pt 高、全圆角，上沿暗内影 + 下沿亮边。macOS 13 没有原生 inner shadow，用「描边 + blur + 渐变 mask」手搓：描边画在形状内侧，blur 后用上下渐变 mask 把亮暗分到两边。
- **组内 / 全局 改成纯 SwiftUI 分段控件**，从搜索框内部挪到右侧独立成一段：内凹底槽 + 一颗浮起的选中滑块。原生 `Picker(.segmented)` 没法接管这套质感，索引换成自绘。
- **图标按钮（外观 / 插件 / 键盘 / 设置）改为 32pt 微凸方块**，页面选择器、打包 / 导入 / 清空 / 自动切换也统一换成凸起表面：normal 默认凸起、accent 用选中填充、destructive 用淡红底 + 红字。
- **自动存储 / 自动粘贴：金属拨杆彻底删掉**（`ToggleLeverView.swift` 已移除），换成设计稿里的浮起面板：左右两区、中间一条竖发丝线，每区是「状态点 + 标题 + 垂直内凹滑道 + 黑色滑块 + 回退 / 重置微凸小按钮」。滑块上下滑动表示开关，26×40pt 滑道、20×17pt 滑块。

### 两种皮肤怎么分

几何和布局**完全共用**，只分颜色：

| | 简洁模式 | 多彩模式 |
|---|---|---|
| 面板 / 凸起 | 中性灰阶，近白但不纯白（纯白留给凸起面，否则凸起和面板分不开） | 底色掺极淡蓝紫 |
| 搜索槽 / 选中滑块 | 中性灰 + 黑滑块 | 品牌蓝紫渐变滑块 |
| 开关滑块 | 黑 | 品牌蓝紫 |
| 回退 / 重置 字色 | 中性墨色 | 跟随状态色（绿 / 蓝） |
| 状态点 | 绿 / 蓝（功能性，两种皮肤都留） | 同 |

延续 hotfix 1 定的规矩：**装饰性上色在简洁模式收掉，功能性状态色两种皮肤都保留**。所以「回退 / 重置」的字色在简洁模式回归中性，而上方的绿 / 蓝状态点保留。

### 实现要点

- 颜色和几何常量下沉到 Kit 层 `NeumorphicPalette.swift`（`NeumorphicPalette` 四套面 + `NeumorphicMetrics` 尺寸），这样 smoke 能直接对数值做断言，而不是靠肉眼看截图。
- App 层 `NeumorphicKit.swift` 提供 `NeuPanelBackground` / `NeuRaisedBackground` / `NeuWellBackground` 三种面，以及 `NeuSegmentedControl` / `NeuVerticalSwitch` / `NeuMiniButton` 三个控件，`.neuPanel()` / `.neuRaised()` / `.neuWell()` / `.neuIconTile()` 四个 modifier。
- 调色踩的坑：浅色面板一开始给了纯白，导致「凸起面必须比面板亮」这条断言直接没救——纯白之上没有更亮的颜色。改成面板 0.973 灰、凸起纯白。多彩深色的滑块填充和多彩浅色的危险红都因为对比度差 0.01~0.8 没过 4.5，各自调暗了一档。
- 「回退」曾被压成「…」：这两颗小按钮的可用宽度由上方 26pt 宽的滑道决定，文本没锁理想宽度就会被判定可截断，加 `.fixedSize()` 解决。

smoke 新增 **NEU** 段共 82 条断言：凸起 / 面板 / 内凹的相对亮度关系、凸起与内凹的对比度区间（要能看出层次但不能重到像描边）、滑块文字与危险操作的可读性、简洁模式必须中性、多彩模式必须带色、以及开关行程 / 圆角 / 阴影方向等几何约束。总量 1212 → **1294 条，失败 0**。

截图实测两种皮肤：工具栏浮起面板、内凹搜索槽、分段滑块、微凸图标方块、开关面板的竖滑道 + 黑 / 紫滑块 + 未截断的回退 / 重置，全部与设计稿一致。

## DMG SHA-256

```
21e26463b738c94b0febfdd07015de8cdc507a4da917c0a3cb0420dc35adff9c
```


## hotfix 4（同版本号重打包）

hotfix3 把新拟物做成了「一块浮起来的板」，用户一眼看出问题：工具栏成了压在内容上的**独立悬浮块**，和下面的卡片区分成两层，界面被生生切开。这轮把分层全部拆掉，只留控件自身的凹凸。

- **删掉工具栏的白色浮动大面板与外阴影。** 连同它的圆角、描边、12pt 悬浮留白一起去掉；工具栏直接坐在画布上。
- **删掉自动存储 / 自动粘贴那一簇自己的小面板。** 它原本是「小卡片浮在大面板上」的两层嵌套，割裂感最重的地方；现在两区直接落在同一张面上，分组关系只靠中间那条发丝线 + 各自标题表达。
- **工具栏与内容区同底。** 顶部背景从 `.regularMaterial` 换成 `AppTheme.windowBackground`——材质层会把窗后内容透上来，明度天然与下面的不透明画布不同，看着就是「上面一条、下面一块」。左右留白也从面板内边距换成 `AppTheme.pagePadding`，与卡片区同值，工具栏里的控件因此与下面第一列卡片左右对齐。
- **新拟物质感全部保留**：内凹搜索框、微凸图标 / 操作按钮、内凹滑道 + 微凸滑块、按下压平阴影，一个没动。层级感现在由控件表达，而不是由一块板表达。

这轮真正的坑在颜色，不在布局：原来的 `well`（内凹底）是相对**面板**调的，而面板比画布亮；把它直接搬到画布上，浅色档只比底暗 3.5%（搜索框几乎消失），深色档甚至**比画布还亮**——那就不是凹陷，是凸起，光影语言当场翻面。所以：

- 调色板的承载面概念从 `panel` 改成 `ground`（画布），`panel` 这一档整体删除；
- 四套取值（简洁 / 多彩 × 浅 / 深）的 `well` / `wellShade` 全部相对画布重调；
- `ground` 必须与 App 层窗口底**逐值相同**，否则工具栏会重新变成一块颜色略不同的方块——这条写成了断言（简洁模式直接比对 `MinimalSkinPalette.window`；多彩模式让 `AppTheme` 直接引用 `NeumorphicPalette.ground` 保证同源）。

smoke 从 1294 增至 **1302**，新增「凸起 / 内凹要在画布上读得出来」与「承载面 = 窗口底」两类断言。

DMG SHA-256：`4d07858ffea5dec31437eb67ac494bfe15420eb45d233b5a999832d844382a77`


## hotfix 5（同版本号重打包）：按设计稿 1:1 还原按钮 / 搜索框光影

hotfix3 的新拟物只做对了「形」，没做对「光」。用户逐条对照设计稿指出的五处差距全部落地：

1. **按钮填充改成与画布同色**（简洁浅色 = #F2F2F3，四套档位全部 `raised == ground`）。之前按钮是纯白、画布是浅灰，色差本身就成了边界，光影反而是配角——这正是「像一片薄片叠在上面」的根因。新拟物的按钮必须是**从底板上鼓起来的同一块材质**。
2. **补上左上白高光**：offset (-2, -2)、blur 4、白 80%。它承担「材质厚度」那一半观感，缺了按钮只剩一团投影、边缘发平。
3. **右下投影改柔和**：offset (4, 4)、blur 12、黑 9%（原来是 (0, 3) + blur 10、黑 11%）。x 偏移为 0 意味着纯垂直下坠，读起来是「悬空掉影子」而不是「一束光从左上打过来」。
4. **去掉描边**：0.8pt 的 hairline 降到 0.5pt / 黑 3%（深色档保留 8% 白——那里可用的白高光只有 7%，全去掉按钮会消失在黑底里）。
5. **搜索框真正凹下去**：上/左内阴影（offset (2,2)、blur 5、黑 8%）+ 下/右内高光（offset (-2,-2)、blur 5、白 60%），并去掉原来那圈灰描边——描一圈边等于把「凹陷」画成「一个有边框的浅色块」。

顺带删掉了 `raisedHighlight`（凸起表面的竖向渐变高光色）：渐变上端是纯白，等于把第 1 条的色差换个形式请回来。

两个不写断言就会反复踩的点，已钉进 smoke（1298 条全绿）：
- SwiftUI 的 `.shadow(radius:)` 是**高斯 sigma**，不是设计稿的 blur 直径，差 2 倍。断言要求投影 radius 落在 5～7（对应设计稿 blur 10~14），且必须 ≥ 高光 radius 的 2.5 倍（投影柔、高光贴边）。
- 「光源在左上」在代码里唯一的落点就是四个偏移量的符号：高光 (负, 负)、投影 (正, 正)，且投影 x ≠ 0。
- `raised == ground` 本身也是断言——这条与「调亮一点让按钮更清楚」的直觉正好相反，最容易被顺手改坏。

副作用修正：凸起不再是纯白后，「凸起 vs 内凹」的明度差全靠内凹自己撑，多彩深色档只剩 1.044 对比度（搜索框在黑底上消失），内凹压深到 #090A10。


## hotfix 6（同版本号重打包）

三件事：一个被误判成「样式绑定」的窗口问题，一个设计稿细节，以及一个**首帧时序 bug**（后两者其实同源）。

### 1. 新开 App 工具栏漂浮 / 切一次皮肤才正常、但阴影变少 —— 同一个 bug

用户报的是两个现象，根因只有一个：那些**半透明的阴影 / 描边 token** 当时写成

```swift
static var dropShadow: Color {
    AppTheme.isDarkAppearance ? .black.opacity(0.55) : .black.opacity(0.09)
}
```

也就是在 **body 求值那一刻**读 `NSApp.effectiveAppearance`，把结果**烘死**进视图里。于是：

- **首帧**：SwiftUI 的第一帧比 `applicationDidFinishLaunching` 里的 `applyAppAppearance()` 更早，
  此时 `NSApp.appearance` 还没被设成用户选的浅色（App 默认主题是深色）。按钮拿到的是**深色档投影**：
  黑 55% / radius 6 / (4,4)——在浅色画布上就是一坨浓重外投影，正是「悬浮漂浮」。
- **手动切一次皮肤**：`.id(skin)` 整树重建，此时 appearance 已正确，token 重算成浅色档的黑 9%。
  所以「切完就正常了」和「切完阴影少了一些」**是同一件事**：前者是错的（55%），后者才是设计值（9%）。

修法不是加 `onAppear` 补刷，而是把这些 token 全部包成 **动态色**（`NSColor(name:dynamicProvider:)`）：
明暗解析交给 AppKit 在**绘制时**做，与 body 何时求值彻底解耦——首帧就是对的，重建也不会变。
表面色（ground/raised/well…）一直走的就是这条路，所以它们从来没出过这个问题；出问题的恰好是
后来新加、图省事写成三元表达式的那几个半透明 token。

顺带把 `applyAppAppearance()` + `AppSkinCenter.syncFromDefaults()` 提前到
`applicationWillFinishLaunching`，让首帧的 AppKit 界面也是对的（第二道保险）。

### 2. 按钮双侧描边（设计稿 image-a4ad5438）

外阴影只交代「按钮周围的空气」，交代不了「按钮自己有多厚」。补上两圈方向相反的描边：

- 右下 **1.5pt 灰边**（约 #CCCCCC / 50%）= 凸起物的**侧面厚度**
- 左上 **1pt 白边**（70%，再往左上挪 0.5pt）= **受光的棱**，锐利不模糊

两条边都用方向渐变 mask 限制在各自半圈——否则就成了「描了一圈双色边框」，那是边框不是体积。
smoke 钉住「厚度边必须比高光边宽」：等宽就会退化成边框。

### 3. 简洁模式「标题栏消失」

代码里没有任何一处按皮肤改 `styleMask`。真正的原因是标题栏的**观感**依赖它背后透出来的东西：
多彩模式那层复古海报氛围底有纹理和渐变，标题栏区域于是有明显材质分界；简洁模式换成纯色底，
标题栏与内容区同色同质，那条带子就「看不见了」，只剩三颗按钮悬在一片纯色上。

所以修法不是解绑一个不存在的绑定，而是**显式声明标题栏必须由 AppKit 自己画**：
`titlebarAppearsTransparent = false`、`titleVisibility = .visible`、去掉 `.fullSizeContentView`，
并在切皮肤后重跑一次，防止 SwiftUI 重建窗口时改回去。两种皮肤下窗口 chrome 从此完全一致。

smoke：1302 条全绿。
DMG SHA-256：`045c9bb371882c7c99c34f74360962cae4572d38264ba7a9a1feeefe19e9671b`
