# ClipSlots v2.11.5 发布说明

本版只做一件事：**把轮盘的硬边扇形换成花瓣式圆角卡片**。

v2.11.4 之前，轮盘是一块被切开的披萨：扇区两条径向直边直接顶到邻居，靠一条 1pt 白线划界。信息都在，但十格挤成一整块，谈不上「一格一张卡」。本版改成参考图那种径向花瓣布局——每格是一张带大圆角、四周留白的独立卡片，靠留白而不是线条分界。

## ✨ 花瓣式扇区

- 扇区改为**圆角花瓣**：外弧 + 内弧 + 两条侧边，四个角都倒圆（切点距离 18pt，视觉半径 ≈ 15pt）。内弧比外弧短得多，内侧圆角会自动收紧，因此花瓣天然呈「外圆内窄」的叶片轮廓。
- 花瓣之间留 **5pt 恒宽通道**。
- **径向分隔线整组删除**。花瓣之间已经有一条 5pt 的物理留白，再在通道正中画一条白线等于把留白重新填满。删线省下的视觉预算改成每片花瓣一圈 0.7pt 轮廓——同样是「划界」，画在卡片自己的边上才是卡片语义。
- 花瓣内沿离中心 hub 的距离从 1.5pt 抬到 **4pt**：硬边时代扇区内沿几乎贴着 hub 圆环（两者本来共用同一条分隔线语言），花瓣化之后 hub 是独立元素，10 片花瓣的内侧圆角必须离开它，否则看起来像「花瓣长在轮毂里」。
- 静息态填充整体提浓（浅色 0.10/0.18 → 0.17/0.30，深色 0.018/0.045 → 0.05/0.10）。原来的极淡填充是为硬边扇形调的：那时靠分隔线划界，填充只需隐约区分「空 / 有内容」；有了通道之后，填充要独自承担「这是一张卡片」的全部表达，0.018 的白在深色磨砂上根本读不出边界。
- **磨砂玻璃背景一行未改**，圆盘底、材质、内外描边、光晕、内阴影全部沿用；只有扇区形状与浓度变了。

## 🔬 间隙为什么是「恒宽」而不是「固定角度」

这是本版唯一真正需要想清楚的几何问题，也是最容易做错的地方：

- **固定角度内缩**（θ ± k 度）实现最省事，但间隙宽度随半径线性增长。按本 App 的实际比例（内半径 48pt / 外半径 178pt、10 槽位），同一个 2° 内缩在内圈只有 1.6pt、在外圈是 6.2pt —— 内圈两瓣几乎粘住、外圈裂开一道大口子，看着就是做坏了。
- **固定垂直距离内缩**（本版做法）：花瓣侧边是一条与原径向边平行、垂直距离恒为 `gap/2` 的**直线**，于是从内到外整条通道宽度恒等于 5pt。

代价是花瓣的角度边界成了半径的函数：`Δ(r) = asin(h/r)`，内圈 Δ 大、外圈 Δ 小 —— 这也正是「花瓣形」的来源，不是副作用。

圆角用**二次贝塞尔**、控制点放在被切掉的几何拐角上：曲线在两个切点处分别与相邻两条边相切，接缝光滑，而且不会像 `addArc(tangent1End:)` 那样在「直线接圆弧」的场合退化失败（CGPath 的切线圆弧只支持两条直线相接）。

## 🧭 文字方向

参考图的另一个要点是「标签不随扇区角度旋转」。核对现有实现后确认**本 App 从来没有旋转过文字**：编号、标签、缩略图早在 v2.11.0 就各自沿扇区中轴线取独立极坐标锚点，内容本身始终水平。本版因此没有动文字方向，只做了一件相关的事：

- 文字块与缩略图的宽度改按**花瓣内缩后**的张角计算。花瓣侧边向内让了 2.5pt 垂距，标签若还按满张角撑开，就会压在圆角边上甚至探进通道 —— 那正是「留白」被视觉噪声吃掉的方式。10 槽位、r=120 处实测可用宽度收窄 5.5pt。

## 🔒 几何住在 Kit，不变量由 smoke 钉住

新增 `ClipSlotsKit/RadialPetalGeometry.swift`：纯函数产出一串路径基元（move / line / quad / arc / close），SwiftUI 侧的 `PetalSegmentShape` 只负责翻译成 `Path`，**不参与任何计算**。理由与 v2.11.0 的缩略图布局一致——本机跑不了 App 层测试，而花瓣的失败模式全都是「看起来差不多但其实越界」，必须能被断言。

花瓣几何相关断言让 smoke 从 878 涨到 **1134**（多数来自逐片数 / 逐半径的枚举展开），覆盖：

- **楔形包含**：1~12 片全枚举，每片花瓣的所有路径点（含贝塞尔控制点、圆弧采样点）都必须落在自己的角度区间内、不越内外半径。
- **通道恒宽**：四个拐角到自己那条原始径向边的垂距必须恒为 `gap/2`；相邻两片在内 / 中 / 外三个半径处的实测通道宽度都必须 ≈ 5pt。同时断言「内圈的角度内缩必须显著大于外圈」——若两者相等就说明退回成了固定角度内缩。
- **圆角不自交**：即使请求 999pt 的圆角，也必须被收紧到不超过所在弧长的一半、且同一条侧边两端的切点不得越过对方。
- **退化桥接**：`gap = 0, trim = 0` 时必须精确退化回老的硬边扇形（拐角回到原始扇区边界角、贴内外半径），让日后想回退时有据可依。
- **窄扇区收紧**：一页 40 个组（9°/片）时间隙必须自动变窄，收紧后两侧内缩合计不得吃掉超过 60% 张角。
- **外弧让路**：「上次粘贴」外弧的端点内缩改由花瓣参数决定，断言它必须落在花瓣的角度区间内、且比硬边模式更短。否则弧的两端会探出圆角、悬在通道上方——一条「比花瓣本身还长」的高亮弧看起来就是渲染出错。

## 🤝 行为与逻辑零改动

- 悬停高亮（v2.11.4 的统一冷灰蓝 @0.25 + 描边 @0.70）、「上次粘贴」外弧与底栏胶囊的槽位色跟随、「全部粘贴」的中性灰、缩略图 / 附件角标 / 悬浮预览面板、组扇区模式的蓝色高亮 —— 全部沿用，本版只改形状与浓度。
- **命中判定仍按角度**，不按花瓣形状：鼠标划到通道里也会命中就近的那一格。故意如此 —— 5pt 通道若成了死区，快速划选时会出现「明明扫过去了却没选中」的顿挫。

## ✅ 验证

- `swift build` 通过。
- `swift run ClipSlotsKitSmokeTests`：**通过 1134，失败 0**（v2.11.4 为 878）。
- 形状定稿走的是**离屏渲染对比**：把 Kit 的同一套公式在 Python 里复刻，横向渲染 gap 4/5/6 × trim 14/16/18/20 × 内缩 1.5/4/5 的组合，以及 1/2/3/6/10 片的极端片数，挑出 gap 5 / trim 18 / 内缩 4 这一组。
- 版本号一致性：`Info.plist` / `AppVersion.fallback` / `CLI_VERSION` / SKILL.md 均为 2.11.5，smoke 里的 CLI-App 版本同步断言已跟进。
- 本机覆盖安装验证：App 版本 2.11.5，`clipslots --version` 返回 2.11.5，进程正常存活。

## 📦 校验

```text
SHA-256 (ClipSlots_v2.11.5.dmg) = 462316a184290fdbd5fc3bc5ee394718014b26b5051178991ef56a6337490c4b
```

DMG 为 ad-hoc 签名（`SKIP_NOTARIZE=1`），未走 Developer ID 公证；浏览器下载后如遇 Gatekeeper 提示，右键「打开」或 `xattr -dr com.apple.quarantine /Applications/ClipSlots.app` 即可。自动更新会在下载后按上表哈希校验（v2.10.67 起）。
