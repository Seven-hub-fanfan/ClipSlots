# ClipSlots v2.16.1

v2.16.0 的画布视觉是照 TapNow 量出来的，但**在你实际用的浅色主题下，它有一半是坏的**。
这一版把它修对了：重新打开本机 TapNow 对照，逐帧抓窗口、逐像素采样，把 v2.16.0 没落地的顶边做实，
顺带修掉三个真实 bug。

## 画布浮层「白底白字」（这一版最该修的）

浅色主题下，画布上的项目切换器、底部工具栏、缩放条、右上按钮**全是空白胶囊，文字完全看不见**
（采样底色 255,255,255，文字是白的）。

原因是 v2.16.0 只统一了**墨色**没统一**底板**：画布墨色写死成白（TapNow 画布恒定纯黑），
而浮层底板仍走随主题变的皮肤色，浅色主题下就是白底 + 白字。

修法不是逐个改那 59 处 token 调用，而是把整棵画布子树的配色方案钉成深色 ——
各皮肤的深色档本来就是设计过、带对比度断言的那一套，直接用它。

## 画布顶边真的黑到窗口边缘了

v2.16.0 写的是「标题栏钉成深色」，实测**根本没生效**：标题栏始终是亮度 231 的浅色横带，
标题「ClipSlots」照旧压在纯黑画布上方。

查下去是两件事：

1. `AppDelegate` 里有个把窗口 chrome 掰回「不透明标题栏」的逻辑，它在启动后 2s 内和**每次切换皮肤**时跑，
   把画布刚钉好的设置又改回去了。现在它认「画布是否在台上」这个状态，不再抢。
2. 更关键的是，`titlebarAppearsTransparent` 和窗口 appearance **根本不是我们能写的**——
   SwiftUI 每次环境更新都会按自己的规则重写回去（swizzle setter 抓到了调用栈）。
   手写的值同一帧读回来是对的，隔一帧就被刷掉，所以 v2.16.0 那条路是死路。

改成用 SwiftUI 自己的开关（`.toolbarBackground(.hidden)` + `.preferredColorScheme`）之后，
纯黑一直铺到窗口顶边、点阵不再被切掉 28pt、红绿灯浮在画布上、标题文字让位 —— 这才是 TapNow 的顶边。
离开画布，标题栏、标题文字、主题全部原样还原（来回切两轮逐档验过）。

## 修掉的 bug

- **每进出画布一次，窗口就长高一截**。改窗口样式会连带改内容区高度，SwiftUI 紧接着按内容尺寸反推窗口大小。
  实测连续三次启动：904 → 932 → 949。现在动样式时把窗口尺寸原样按回去，往返两轮尺寸不动。
- **进过画布后窗口就没名字了**。标题只在画布分支里置空，切回编辑模式 SwiftUI 保留最后的值，
  窗口菜单和调度中心里都是一个无名窗口。现在标题按模式取值。
- **画布文本节点可能被清空**。编辑框的草稿只在「进入编辑态」那一刻初始化；如果卡片在已是编辑态时被重建
  （槽位重排会触发），草稿是空串，失焦提交就把原文清掉了。现在挂载时也同步一次。
- 画布模式下侧栏标题与浮动控件避开顶部 28pt 的红绿灯与标题栏拖拽区。

## 说明

- 本版只在 `2.16.x` 线上走，不含也不影响并行开发的 `2.17.x`。
- 自测：`swift build` 通过，零依赖 smoke **32236 条断言全通过**。
- 实机验收全程用窗口层截图 + 像素采样，不靠肉眼判断。

---

**DMG SHA-256**

```
33a44aa3b22f7e8a818453d0e1ff455f7be7a83d60d5c2433cb84daa7ea54513
```
