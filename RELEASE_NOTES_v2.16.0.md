# ClipSlots v2.16.0

本版只做一件事：**把画布的视觉与手感照着 TapNow 重做一遍**。

上一版（v2.15.0）把节点类型补齐了，但你的反馈是「还是几乎不沾边」。这次没有再靠描述猜，而是直接打开本机
`/Applications/TapNow.app`，截图 + 逐像素采样把它的底色、点阵、卡片、连线、端口、工具条量出来，再按量到的数照着改。

## 画布底

- 背景改**纯黑** `#000000`，不再跟随主题色。
- 网格从实线改成 TapNow 那样的**点阵**：间距 16pt、点色 `#4A4A4A`、点径 1.2pt。
- 画布模式下**窗口标题栏也钉成深色**。此前浅色主题时是一条亮度 241 的白条直接压在纯黑画布上方，
  是整窗唯一一处大面积亮区。离开画布自动还原，不影响你的主题偏好。

## 节点卡

- 卡片**去掉描边、去掉标题栏**，圆角 12pt，填充 `#1F1F1F`（选中态 `#343434`）。
- 图片 / 视频**铺满整张卡**，不再是「卡里嵌一张小图」。
- 节点名字移到**卡外上方的小名签**，和 TapNow 一致。
- 操作按钮（入库 / 全屏）、媒体信息、字数角标改成**只在 hover 时出现**，静息态是干净的内容块。
- 操作条改成卡片上方的**浮动胶囊**，高 44pt，深色填充无描边。

## 连线与端口

- 连线改成 **1pt 灰色贝塞尔**：去掉箭头、去掉起点圆点、去掉运行中的流动虚线，hover / 选中 / 运行中只改亮度。
- 端口改成卡片**左右外侧的 ⊕ 圆**，直径 18pt、圆心距卡边 28pt；连上之后中心变实点。

## 手感

- **拖动不再吸附网格**。之前拖的时候预览是连续的、一松手却吸回格点，看着像「回跳」。现在只做 0.5pt 取整清理浮点噪声，
  多选拖动的相对间距保持不变。（新建节点的落点吸附保留。）
- 拖动节点时光标切成抓手。

## 顺手修掉的

- 浅色主题下画布上的文字**看不见**：画布 chrome 的文字色此前会跟随系统浅色解析成近黑，压在纯黑画布上等于消失。现在钉死成深底可读的亮色。
- 提示词编辑态**只见光标不见字**：同一个原因，编辑器改为固定亮色文字 + darkAqua 外观。
- 删掉 v2.15.0 遗留的死代码 `CanvasMediaNodeCard.swift`（已无任何调用点）。

## 已知差距

TapNow 是 `fullSizeContentView`，黑到窗口顶边、只剩红绿灯浮在画布上。我们这版止步于深色标题栏（实测 49,49,49）：
要做到黑到顶边，得同时给侧栏和画布 chrome 补一条约 28pt 顶部内缩来避开红绿灯，属于布局改动，留给后续版本。
ADD NODE 菜单样式、视口平移惯性也还没追到 TapNow 的水平。

## 验证

- `swift build` 通过
- smoke 测试 **32236 通过 / 0 失败**
- DMG 已本机安装验证，CLI 返回 `2.16.0`
