# ClipSlots v2.10.96

## ✨ 新功能：Cmd+Shift+Z 反向撤回（重做）

v2.10.95 上了 10 步撤销，但撤销过头就回不来了。这一版补上对称的另一半：

- **快捷键**：`Cmd+Shift+Z` 重做最近一次被撤销掉的改动，可以连按。
- **来回走**：`Cmd+Z` / `Cmd+Shift+Z` 可以在同一条时间线上反复前后移动，状态精确回到对应时点。
- **按组独立**：重做链同样每个槽位组各自维护，最多 10 步；当前组没有重做记录时提示
  「请切回原分组」，绝不把 A 组的历史写进 B 组（沿用 v2.8.7 D 不变量）。
- **新改动作废重做分支**：撤销之后又做了新的槽位改动，说明时间线已分叉，该组的重做记录会
  被清空（与所有编辑器的 undo/redo 语义一致），不会重做出一个「不该存在的未来」。
- **附件不断链**：撤销时会把当前状态的外置附件字节就地内联进重做快照（单次 64MB 预算），
  因为整组回写会换掉槽位目录、原字节可能随之消失；重做后附件仍可正常预览/粘贴。
- **跨重启可用**：重做栈持久化到
  `~/.local/share/clipslots/special_slots/.undo/redo_stack.json`，重启 App 后仍可重做。
- **不抢文本编辑器的重做**：焦点在文本框/文本编辑器且它自己有重做记录时，`Cmd+Shift+Z`
  交还给系统原生文本重做，行为与 `Cmd+Z` 的分流完全对称。
- **提示更清楚**：撤销到最后一步时浮层会提示「Cmd+Shift+Z 可重做」。

## 🛡️ 保持不变的既有不变量

- 撤销/重做都只回写到快照被抓取的那个槽位组，不污染其它组。
- 缩略图身份（contentId + updatedAt）契约不变；回写时整组失效一次缩略图缓存。
- 存储层 STG-2 幽灵组护栏、跨进程写锁、`.trash` 软删除逻辑均未改动。
- 撤销栈本身的 10 步 / 全局 30 条上限、去重与迁移逻辑未改动。

## 🧪 测试

- `swift build` 通过。
- `swift run ClipSlotsKitSmokeTests`：**99 / 99 通过**（新增 17 条断言：撤销↔重做双栈来回走
  步数守恒、重做栈 10 步上限、「新操作作废重做分支」只影响当前组、重做栈 JSON 跨重启往返，
  以及「撤销 → 重做」的整组内容回写往返）。
