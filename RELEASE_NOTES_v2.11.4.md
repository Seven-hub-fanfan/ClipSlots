# ClipSlots v2.11.4 发布说明

本版只做一件事：**重新划分圆盘菜单的颜色分工**——槽位色负责「这是哪一格」，中性色负责「这一格被选中 / 这是个通用动作」。

在 v2.11.3 之前，主界面每张卡片都有自己的槽位色（绿 / 琥珀 / 橙 / 品红 / 蓝循环），但一进圆盘全被抹平成同一种系统强调色蓝。本版先把槽位色接进圆盘，再经过几轮上手调校，收敛成如下分工：

| 元素 | 用色 |
| --- | --- |
| 扇区外沿「上次粘贴」弧 | 槽位色（身份） |
| 底栏「上次粘贴」胶囊 | 槽位色 @0.28 + 同色描边 @0.95，文字黑白自动（身份） |
| 悬停 / 聚焦扇区 | **统一低饱和冷灰蓝** @0.25 + 同色描边 @0.70（状态） |
| 底栏「全部粘贴」按钮 | **系统中性灰**，跟随亮 / 暗外观（通用动作） |
| 静息扇区、组扇区模式 | 原白玻璃三档 / 原蓝色高亮，未改动 |

## ✨ 悬停扇区：统一冷灰蓝，不再跟随槽位色

- 悬停（或被「上次粘贴」按钮程序化聚焦）的扇区，填充与描边统一用一支**低饱和冷灰蓝**：浅色 `HSB(220°, 18%, 88%)` ≈ `#B8C5E0`，深色 `HSB(224°, 26%, 84%)` ≈ `#9FADD6`。铺 @0.25 后浅色下约 `#E4E7F0`、深色下约 `#40444F`——只是「亮了一档 + 微微偏蓝」，不产生第六种颜色。
- 为什么放弃跟随槽位色：圆盘上原本有三处同时在讲槽位色（悬停填充、外沿弧、底栏胶囊），10 槽位下鼠标扫一圈就是绿→黄→橙→粉→蓝五色相轮播，「哪一格被选中」这条唯一有用的信息被颜色噪声盖住了。现在职责切开：**身份**归外沿弧与底栏胶囊，**状态**归统一冷灰蓝。
- 也刻意不用 `Color.accentColor`：那是跟随系统偏好设置的（用户可能设成粉、橙、石墨），一跟着变就回到「悬停色不可控」的老问题。
- 描边浓度从 0.52 补到 **0.70**：染色从高饱和槽位色换成低饱和冷灰蓝后，0.52 在浅色下几乎看不出边界（`#D4DBE9` 与白底差不到一档）。填充仍保持 0.25 的轻盈，下层磨砂玻璃与扇区分界线要能透出来；白色高光薄层保留。
- **非悬停扇区不染色**：十格全上色会变成调色盘，反而看不出焦点，所以静息态仍是原来的白玻璃三档（空 / 有内容 / 悬停）。
- 组扇区模式（第二层「切组」视图）保持原来的蓝色高亮——那一层的语义是「组」，不是槽位。

## ✨ 底栏「全部粘贴」改中性系统灰

- 原先用 `Color.accentColor`（0.16 底 / 0.35 描边）。两个问题：accentColor 跟随系统偏好设置，用户设成粉 / 橙 / 石墨时底栏会跟着换色，而它旁边就是跟随槽位色的「上次粘贴」胶囊——两块彩色贴片抢同一条底栏；另一方面「全部粘贴」是**无差别批量动作**，不隶属任何槽位，本来就没有色彩身份可言。
- 现在改成中性灰：浅色下在磨砂玻璃上压一层黑（→ 浅灰），深色下提一层白（→ 深灰），文字色同步跟随外观，与系统默认 bezel 按钮同一路数。底栏的彩色配额留给唯一真正需要它的「上次粘贴」。

## ✨ 底栏「上次粘贴」胶囊跟随槽位色（本版新增，未随 hotfix4 改动）

- 有粘贴记录时，胶囊底色换成**目标槽位的颜色** @0.28（与悬停扇区填充同档的轻染色），描边同色 @0.95 守住按钮轮廓。这样「卡片角标 → 扇区外弧 → 底栏按钮」三处颜色统一，扫一眼就知道上次粘贴的是哪一格，不用再回读文字。
- 文字与图标颜色**按亮度自动选黑或白**，不是写死的白字。
- 尚未粘贴过任何槽位时，保持原来的玻璃灰样式 + 置灰禁用 + tooltip 提示，行为不变。

## 🔬 墨色是算出来的，不是挑出来的

- 「黑字还是白字」走 **WCAG 相对亮度 + 对比度**：先把槽位色按胶囊不透明度与底色做 alpha 合成，再比较合成色与黑 / 白的对比度，取高的那个。
- 刻意不用 `(r+g+b)/3` 或 HSB brightness：那两种算法会把深蓝和亮黄判成同一亮度，必然出现「蓝底黑字、黄底白字」的不可读组合。
- 也刻意不用原色算亮度：半透明胶囊真实观感取决于**合成后**的颜色，用原色会选错。
- 调色板与这套数学已从 App 层下沉到 `ClipSlotsKit/SlotAccentPalette`（零 UI 依赖的纯函数），smoke 覆盖：取色循环（含 slot 0 / 负数越界兜底）、亮度加权方向（绿 > 红 > 蓝）、alpha 合成边界、HSB 往返无损、提亮后色相零漂移、以及**每个槽位在深浅两套外观下墨色对比度 ≥ 4.5:1（WCAG AA）**。以后换调色板或调浓度，读不清的配色会当场测试失败。
- 统一悬停色也进了同一套断言：色相必须落在 200°~250° 冷区间、饱和度 ≤ 0.30、蓝通道必须主导、与每个槽位色「色相差 > 25° 或饱和度不足其一半」（不能只比色相——它本身就是蓝调，与蓝色槽位只差 13°~17°，真正把两者分开的是饱和度）、以及铺 @0.25 后与底色的亮度方向正确且对比度 < 1.9:1（仍是一层轻纱而非实心色块）。
- 这套「算出来」的收益在本版内兑现了两次：胶囊浓度从 0.85 降到 0.65 时，深色模式下第 3、4 号槽位自动从黑字翻成白字；再降到 0.28 后，深色模式五个槽位**全部**翻成白字（对比度 8.0~10.2），浅色模式则维持黑字（10.6~12.3）。全程不需要人肉重挑一次文字色。

## 🎨 圆盘用色调校（本版内 hotfix）

首个 v2.11.4 包上手后发现色太深太闷：浅色模式下外沿弧和底栏胶囊糊成暗橄榄色。根因是圆盘直接吃了主界面卡片那套基础调色板——同一支色，铺成几毫米见方的小角标是「深而稳」，铺成大面积半透明扇区 / 胶囊就往灰里塌。

- 仍跟随槽位色的两处（底栏胶囊底与描边、扇区外沿弧）统一过一层提亮：**色相锁死不动，饱和度 ×1.20、明度朝白抬 45%**。例如浅色琥珀 `#B77C0A → #D78E00`、浅色绿 `#1E8E47 → #0BC14D`、浅色蓝 `#2875B2 → #0F7DD4`。
- 明度用「补足式」`v + (1 - v) × 0.45` 而不是 `v × scale`：后者对本就接近纯白的深色粉彩调色板几乎无效，乘出 >1 还会被截断成失真纯色。
- 色相零漂移是硬约束（smoke 断言 < 0.005 圈），这样圆盘与主界面卡片仍是同一支色、只差一档明度。
- 提亮之后浓度整体再降：悬停填充 0.45→0.25、悬停描边 0.72→0.52→**0.70**（换成低饱和冷灰蓝后又补回来）、底栏胶囊底 0.85→0.65→**0.28**。色本身够亮了，高浓度反而把高亮压成实心色块、盖住下层磨砂玻璃；胶囊底最终与悬停填充同档，槽位色只作一层轻染色，按钮边界改由 0.95 的同色描边守住——底色越淡，越需要一条清晰轮廓线，否则胶囊会在磨砂底栏上「化开」。
- 最后一轮（hotfix4）承认了浓度调不出来的东西：**色数本身就是问题**。悬停扇区退出槽位色改统一冷灰蓝、「全部粘贴」退出 accentColor 改中性灰，圆盘的彩色只剩「上次粘贴」这一条线索。
- **主界面卡片角标 / 连接色点不受影响**，本次只动圆盘。

## ✅ 验证

- `swift build` 通过。
- `swift run ClipSlotsKitSmokeTests`：**通过 878，失败 0**（v2.11.3 为 765，本版 +113）。
- 版本号一致性：`Info.plist` / `AppVersion.fallback` / `CLI_VERSION` / SKILL.md 均为 2.11.4，smoke 里的 CLI-App 版本同步断言已跟进。
- 本机覆盖安装验证：App 版本 2.11.4，`clipslots --version` 返回 2.11.4，进程正常存活。

## 📦 安装

DMG 为 ad-hoc 签名（未走 Developer ID 公证）。首次打开若被 Gatekeeper 拦下，右键「打开」或在「系统设置 → 隐私与安全性」中放行。

```
SHA-256 (ClipSlots_v2.11.4.dmg) = c56568a35d07b02743db676128c2635ab8ac64f993ed87d6117bd1337d57a49d
```

App 内自动更新会校验此哈希（v2.10.67 起）。
