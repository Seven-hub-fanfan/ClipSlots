# ClipSlots v2.11.4 发布说明

本版只做一件事：**把「槽位色」这条视觉线索接进圆盘菜单**。

在 v2.11.3 之前，主界面每张卡片都有自己的槽位色（绿 / 琥珀 / 橙 / 品红 / 蓝循环），但一进圆盘全被抹平成同一种系统强调色蓝——悬停第 1 格和第 7 格长得一模一样。本版让圆盘也讲同一套颜色语言。

## ✨ 悬停扇区跟随槽位色

- 悬停（或被「上次粘贴」按钮程序化聚焦）的扇区，填充与描边改用**该槽位自己的颜色**，不再是统一的蓝。
- 填充 @0.45、描边 @0.72：填充刻意留高透明度，下层磨砂玻璃与扇区分界线仍要透出来；描边比填充实一档，负责「就是这一格」的硬边界。原有的白色高光薄层保留，玻璃感不变。
- **非悬停扇区不染色**：十格全上色会变成调色盘，反而看不出焦点，所以静息态仍是原来的白玻璃三档（空 / 有内容 / 悬停）。
- 组扇区模式（第二层「切组」视图）保持原来的蓝色高亮——那一层的语义是「组」，不是槽位。

## ✨ 底栏「上次粘贴」胶囊跟随槽位色

- 有粘贴记录时，胶囊底色换成**目标槽位的颜色** @0.85，描边同色。这样「卡片角标 → 扇区外弧 → 底栏按钮」三处颜色统一，扫一眼就知道上次粘贴的是哪一格，不用再回读文字。
- 文字与图标颜色**按亮度自动选黑或白**，不是写死的白字。
- 尚未粘贴过任何槽位时，保持原来的玻璃灰样式 + 置灰禁用 + tooltip 提示，行为不变。

## 🔬 墨色是算出来的，不是挑出来的

- 「黑字还是白字」走 **WCAG 相对亮度 + 对比度**：先把槽位色按 0.85 不透明度与底色做 alpha 合成，再比较合成色与黑 / 白的对比度，取高的那个。
- 刻意不用 `(r+g+b)/3` 或 HSB brightness：那两种算法会把深蓝和亮黄判成同一亮度，必然出现「蓝底黑字、黄底白字」的不可读组合。
- 也刻意不用原色算亮度：0.85 的半透明胶囊真实观感取决于**合成后**的颜色，用原色会选错。
- 调色板与这套数学已从 App 层下沉到 `ClipSlotsKit/SlotAccentPalette`（零 UI 依赖的纯函数），smoke 新增 41 条断言：取色循环（含 slot 0 / 负数越界兜底）、亮度加权方向（绿 > 红 > 蓝）、alpha 合成边界、以及**每个槽位在深浅两套外观下墨色对比度 ≥ 4.5:1（WCAG AA）**。以后换调色板，读不清的配色会当场测试失败。
- 当前调色板 10 种组合（5 色 × 深/浅）算下来全部落在黑字：黑字对比度 4.98~9.53，白字仅 2.20~4.22。机制本身是通用的——调色板若改深，会自动切成白字。

## 🎨 圆盘用色统一提亮（本版内 hotfix）

首个 v2.11.4 包上手后发现色太深太闷：浅色模式下外沿弧和底栏胶囊糊成暗橄榄色。根因是圆盘直接吃了主界面卡片那套基础调色板——同一支色，铺成几毫米见方的小角标是「深而稳」，铺成大面积半透明扇区 / 胶囊（0.45~0.85 alpha）就往灰里塌。

- 圆盘的三处用色（悬停扇区填充与描边、底栏胶囊底与描边、扇区外沿弧）统一过一层提亮：**色相锁死不动，饱和度 ×1.20、明度朝白抬 45%**。例如浅色琥珀 `#B77C0A → #D78E00`、浅色绿 `#1E8E47 → #0BC14D`、浅色蓝 `#2875B2 → #0F7DD4`。
- 明度用「补足式」`v + (1 - v) × 0.45` 而不是 `v × scale`：后者对本就接近纯白的深色粉彩调色板几乎无效，乘出 >1 还会被截断成失真纯色。
- 色相零漂移是硬约束（smoke 断言 < 0.005 圈），这样圆盘与主界面卡片仍是同一支色、只差一档明度。
- **主界面卡片角标 / 连接色点不受影响**，本次只动圆盘。
- 墨色决策基于提亮后的颜色重算：10 种组合仍全部落黑字，对比度 5.32~9.97。

## ✅ 验证

- `swift build` 通过。
- `swift run ClipSlotsKitSmokeTests`：**通过 854，失败 0**（v2.11.3 为 765，本版 +89）。
- 版本号一致性：`Info.plist` / `AppVersion.fallback` / `CLI_VERSION` / SKILL.md 均为 2.11.4，smoke 里的 CLI-App 版本同步断言已跟进。
- 本机覆盖安装验证：App 版本 2.11.4，`clipslots --version` 返回 2.11.4，进程正常存活。

## 📦 安装

DMG 为 ad-hoc 签名（未走 Developer ID 公证）。首次打开若被 Gatekeeper 拦下，右键「打开」或在「系统设置 → 隐私与安全性」中放行。

```
SHA-256 (ClipSlots_v2.11.4.dmg) = deb7d1cd1556495afe17d8922c8cea51eab39b54110eec2960f5601447f19533
```

App 内自动更新会校验此哈希（v2.10.67 起）。
