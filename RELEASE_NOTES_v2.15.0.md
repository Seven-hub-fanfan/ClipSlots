# ClipSlots v2.15.0 · 画布节点体系重做（槽位 / 文本 / 图片 / 视频）

## 这一版在回答什么

现有画布上每张卡长得都一样：一叠槽位缩略图 + 提示词 + 入参文件。它当初是"所有节点的样子"，
于是图片节点、视频节点、文本节点三者只在参数面板里有区别，卡片本身讲不出"我是什么"。

这一版把**形态**和**语义**分开：现有那张卡正式降格为**其中一种节点**，叫「槽位节点」；
图片 / 视频拿到一张媒体主导的新卡；文本节点补上它缺的那两件事。

## 槽位节点（原来的通用卡）

- `CanvasNodeKind.slot` 正式命名，**长相一个像素没动**。
- 老画布里的 `.image` 节点在读取时自动迁成 `.slot`（`CanvasDocument` schema 1 → 2）。
  这一步是为了兑现"升级后打开老画布不变脸"——它们画出来本来就是这张卡。
- ADD NODE 菜单里的「槽位节点」从"展开左侧槽位库+弹提示"改成**直接建一张空槽位卡**。
  从槽位库拖已有槽位那条路不变。

## 图片 / 视频节点（TapNow 形态）

- 媒体区吃掉全部剩余高度，aspect-fit 不裁切；提示词收成底部一行摘要。
- 左下悬浮**信息角标**：`尺寸 · 比例 · 时长 · 体积`。
  - 比例按人类档位表**贴档**：`1360×768` 显示 `16:9`，而不是技术正确但没人读得懂的 `85:48`。
  - 体积固定 1024 进制、不走 `ByteCountFormatter`——同一个文件在两台机器上必须显示同一个数。
- 空态按节点选的 `ratio` 画占位框：选了 `9:16` 就看到竖框，不会被读成"参数没生效"。
- 生成中保留上一版图并叠薄雾 + 进度圈（而不是清空成灰块）；失败态叠错误提示。
- 让位顺序与槽位卡相反：卡拖矮时**先砍提示词条**保媒体区，再矮才隐藏文字让媒体独占。

## 全屏预览

- 画布内 overlay，不开新窗口。
- 图片：捏合缩放 `0.2×~8×`、拖动平移、双击复位、顶部 `+ / − / 复位`。
- 视频：AppKit `AVPlayerView` 包装（绕开 SwiftUI `VideoPlayer` 在 macOS 15.7.x 的已知崩溃）。
- 顶部信息条：文件名、路径、媒体角标，以及**入库 / 访达显示 / 关闭**。
- `Esc` 关闭。

## 文本节点

- 操作条新增**复制全文**（对齐 TapNow 的「复制全部」）。只给文本节点：出图节点上那段字是提示词，
  复制它通常是为了改一版，双击全选更直接。
- 卡片右上角显示**字数**。媒体节点有"尺寸 · 体积"，文本节点的等价量级信息就是它——
  25% 缩放下不用点开也能认出哪张是长文。

## 连线显示

- **起点端点圆**：末端本来就有箭头，起点却是线"凭空长出来"，贴着卡边时分不清是从这张卡出发还是路过。
- **入口端口**（左侧中点）：静态只给当前交互节点画一枚；**拖线时给所有合法落点都画**，
  空心=能接、带数字=已有几条上游、实心高亮=正瞄着它。没圆点的卡就是连不上的卡。
- **运行中流动虚线**：下游节点排队 / 生成中时，实线上跑过一串亮点（叠加而非替换，
  替换会让线在开始生成的那一刻看着像断开了）。

## 入库

- 所有节点都兼容入库：画布私有（「未入库」）节点的操作条上出现**入库**按钮，
  一键把内容搬进当前正式槽位组的第一个空槽；全屏态里也能入库。
- 已经在正式槽位库里的节点不显示这个按钮（没有可入的库）。

## 工程

- 新增 Kit 纯函数：`CanvasMediaInfo`（格式化）、`CanvasMediaCardLayout`（媒体卡纵向预算）。
- 新增 `CanvasMediaProbe`（元信息探测，缓存 key 含 mtime + size，重跑覆盖同路径后角标不滞后）、
  `CanvasMediaPick`（卡片展示与全屏预览共用同一套媒体挑选规则，避免"卡上是产物、点开是参考图"）。
- Smoke：`32234` 条全通过，新增 `CANVAS-KIND` / `CANVAS-MEDIA` / `CANVAS-MCARD` 三组。
