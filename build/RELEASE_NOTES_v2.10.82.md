## ClipSlots v2.10.82

本次正式发布聚合了 v2.10.75 → v2.10.82 的一批本地迭代，最终以 v2.10.82 对外发布（自动更新只读 latest）。

### 本版重点（v2.10.82）
- 修复纯附件槽位（正文为空但有附件）中 `(空)` 占位文字未居中的问题：现按水平+垂直居中显示，且不影响有正文文本、图片/视频缩略图与完全空槽位。

### 批次要点（v2.10.75 → v2.10.81）
- **v2.10.81**：根治切换深浅色后顶部氛围层卡旧色——去除海报背景静态栅格缓存，背景与 header 材质层强制身份失效（`.id(scheme)`），保留 resize 期间纯色降级。
- **v2.10.80**：切主题卡旧色首次尝试（缓存 key 并入主题维度），实测无效，由 v2.10.81 根治。
- **v2.10.79**：修复自动存储/自动粘贴摇杆拨动卡顿——拨杆簇观察下沉为局部子视图，`recomputeAutoPreviews` 磁盘扫描后台化 + 令牌防时序错乱。
- **v2.10.78**：搜索框整体右移紧挨图标簇。
- **v2.10.77**：切组 tab 交互反馈 + 搜索框收窄。
- **v2.10.76**：性能三阶段（交互状态下沉 TransientUIStore + SlotCardView Equatable、背景栅格化缓存、动画体系统一 Anim token）。
- **v2.10.75**：拖拽 resize 期间彻底冻结布局，消除每帧重排。

### 完整性校验
SHA256: 47137d95f54ce135a3bf3f9a74b27fed04d31b5fc26f803d70e4a37a7771ae3b
