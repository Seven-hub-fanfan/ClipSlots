# ClipSlots v2.10.94

## 🐞 Bug 修复

- **拖入文件时槽位卡片左上角冒出一个小色块**
  卡片顶部的槽位配色装饰横线（34×3 Capsule）此前贴着卡片上沿直接叠在卡片上、**没有按卡片圆角裁剪**。
  卡片圆角半径 24，在 y≈0 处左上角正被圆角切掉，因此横线最左侧一小段其实落在卡片形状之外。
  静止态下它压在窗口背景上、与横线连成一体几乎看不出；但 drag-over 时导入蒙层（同样圆角的
  ultraThinMaterial）只能压暗**圆角之内**的部分，圆角外那一小段饱和度不变，于是以孤立小方块的
  形式凸在左上角。
  现在装饰横线被裁剪在卡片圆角内，位置/尺寸/配色完全不变，左端由圆角自然切齐，任何状态下都不会溢出。

## 🔧 其他

- 修正 CLI 版本号：`clipslots version` 此前仍硬编码 `2.10.92`，现已同步为 `2.10.94`。

## ✅ 验证

- `swift build` 通过
- `swift run ClipSlotsKitSmokeTests`：**59/59 通过**
- 本地打包（adhoc 签名）+ 装机验证：App 与 CLI 版本号均为 2.10.94
