# ClipSlots v2.10.65

## 修复（承接 v2.10.64）：主体图片切组后缩略图卡住，需"切走再切回"才刷新

### v2.10.64 为何没能根治
v2.10.64 把网格单元格 `.id` 从「仅槽位序号」改成「页面+组+槽位+渲染令牌」，解决了**跨组复用同一单元格**的问题。但真正让本 Bug 残留的机制还没被覆盖：

- 切组时为了丝滑，`slots` **不清空**（v2.10.47 设计），新组的单元格会先拿上一组还留在内存里的内容来渲染（`slotCardView` 直接读 `store.slots[slot]`，而 `specialSlotId` 已是新组）。
- 随后异步读盘把新组内容提交进 `slots`，但此时单元格的 `.id` **没有变**（页面/组/槽位/令牌都没变）。于是 `SlotThumbnailView` 的 `@StateObject`（里面持有的正是**已解码的上一组 NSImage**）不会被重建，旧缩略图就"卡"在那里。
- 只有再切到别的组又切回来，单元格身份才彻底重建、`@StateObject` 才被丢弃 → 表现为"必须切走一个组再切回来才刷新"。

### 本次修复
把**内容版本**（`contentId` + `updatedAt`）也编进网格单元格的 `.id`：

```
.id("页面|组|槽位|contentId|updatedAt|渲染令牌")
```

- 异步读盘一旦把新组内容提交进 `slots`，该槽位的 `contentId` 随之变化 → 单元格 `.id` 变化 → **整格重建**（含全新的 `loadState`）→ 旧组已解码的缩略图被丢弃，按新内容重新加载。**不再需要手动切走切回。**
- 空槽（`nil`）用固定 `"empty"` 兜底，避免 `SlotContent()` 每次渲染生成随机 `contentId` 导致的抖动式无限重建。
- 内容版本是稳定判据（持久化于 meta；空槽由存储层缓存保持稳定），只有内容真正变化时才触发重建，正是需要刷新缩略图的时机。

### 验证
- `swift build` 全量编译通过。
- `swift run ClipSlotsKitSmokeTests`：23 项 smoke 测试全绿。
