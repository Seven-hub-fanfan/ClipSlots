# ClipSlots v2.14.0

## 画布私有内容彻底搬出槽位库

上一版（v2.13.0）的「未入库 / 暂存区」虽然在界面上做了过滤，但它们**本质上还是槽位组**——登记在
`special_slots/index.json` 里，占着槽位组体系，过滤一漏就冒到默认页面的组标签栏上。这一版把它从
「隐藏」改成「不存在」。

### 1. 画布私有内容独立成库

- 新增独立存储根 `~/.local/share/clipslots/canvas/private_slots/`，画布上没有对应槽位的节点内容全部住在这里。
- 槽位库索引里**一个私有组都没有**：不占用槽位页面、不占用槽位组数量、不出现在组标签栏、不进圆盘、不吃快捷键。
- 存储层按组 id 路由（`SpecialSlotStorage.storage(forGroupId:)`）：画布私有组 → 私有库，普通槽位组 → 槽位库。
- 在槽位库上创建画布私有组现在会直接抛错——这条 guard 是新不变量的最后一道门。

### 2. 「暂存区」这个叫法没有了

每个画布项目的私有分区统一叫「未入库」。项目身份由顶部的项目切换器表达，不再用第二个名字去区分同一种容器。

### 3. 删除节点，内容当场消失（而且 Cmd+Z 还能拿回来）

v2.13.0 为了保住撤销能力，给「撤销栈还引用着的内容」开了豁免，结果是删掉一批节点、侧栏一条都没少。

这一版把「可见」和「可撤销」解耦：删除时把内容**整棵目录搬进不可见的撤销暂存区**。

- 用户视角：列表立刻少一条，符合「在画布中删除就该消失」。
- 撤销视角：Cmd+Z 把内容原样搬回——搬的是目录而不是快照，附件外置文件、Label、手动缩略图字节级无损。
- 撤销恢复不覆盖：如果那个槽位号已经被新内容占用，宁可不恢复，也不盖掉用户新写的东西。
- 上一次会话遗留的暂存内容在启动时整体进 `.trash`（30 天可恢复），不是直接删。

### 4. 老数据自动迁移

升级后首次启动会把 `__unfiled__` 和 `__canvas__<projectId>` 从槽位库整棵目录搬到画布私有库，并修好
主索引里指向它们的当前组 / 预览选中 / 快捷键归属 / 自动存储游标。

- 搬的是目录而不是"导出再导入"，附件与 Label 不丢。
- 搬不动就整组放弃并保留在老位置，绝不出现「索引说没有、内容还在」的孤儿状态。
- 幂等：已迁移过的索引是不动点，不会每次启动都重写。

## 安全性

- 对账（可见内容 == 当前节点引用的内容）带两道安全闸：画布文档加载失败时一个都不动；非画布私有组一个都不动。
- 本版新增 / 重写画布私有相关断言约 60 条，全量 smoke 32175 条通过。

## 校验

DMG SHA-256：见下方 `SHA256` 行（自动更新会用它做完整性校验）。
