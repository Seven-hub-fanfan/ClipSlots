# ClipSlots v2.11.1 发布说明

圆盘菜单（RadialMenu）增强专版：把「这个槽位有没有附件 / 我刚才粘的是哪一格」这两件原本只能回主界面才看得到的信息，直接搬到圆盘上；悬停预览窗也终于能看见附件了。

## ✨ 新增

### 1. 扇区附件角标
- 槽位挂了附件时，扇区编号旁出现一枚回形针角标，鼠标悬停显示「该槽位有 N 个附件」。
- 角标染成与主界面卡片「附件 N」胶囊一致的**品牌蓝紫色**，圆盘其余配色（扇区底色、hover 高亮、底栏胶囊）保持不变。
- 判定只读内存中的 `attachments` 字段（O(1)），不触发任何磁盘 I/O，划过圆盘不会掉帧。

### 2. 扇区「上次粘贴」标识
- 上一次粘贴过的槽位，扇区**外沿**会描一段高亮弧，颜色与该槽位在主界面的角标同色。
- 只描外弧、不描径向边：避免与相邻扇区的分隔线重合而看起来像「选中了两个扇区」。
- 说明：之所以不做成编号行里的第二枚角标——10 槽位布局下编号行所在半径的弦宽只有 ~57pt，而「编号 + 串联色点 + 两枚角标」需要 70pt，必然越界到邻居扇区。该结论已固化为 smoke 断言。

### 3. 底栏「上次粘贴」定位按钮
- 圆盘底部工具栏在组名与「全部粘贴」之间新增 `checkmark.circle.fill` 按钮，图标与主界面卡片上的绿色「上次粘贴」胶囊保持一致。
- 点击后**全程留在圆盘内**：静默切到目标页 + 目标组 → 对应扇区自动高亮 → 悬浮预览窗同步显示该槽位内容。圆盘不关闭、主窗口不抢焦点、当前手上的操作不被打断。
- 组数据异步加载完成后预览会自动补一次刷新；鼠标重新划入圆盘或点击任意扇区即释放这次程序化聚焦，回归正常 hover。
- 尚未粘贴过任何槽位时按钮置灰，悬停提示「尚未粘贴过任何槽位」。

### 4. 悬浮预览窗支持附件预览
- 悬停带附件的扇区时，预览窗底部新增附件条：图片附件出**真实缩略图**（最多 3 张，超出折成「+N」），非图片附件出「语义图标 + 文件名」小卡（按 UTType 区分视频 / 音频 / PDF / 压缩包 / 代码 / 文本）。
- 主视觉优先级：手动封面图 → 槽位主体内容 → 图片附件缩略图 → 文本 → 空。主体为空、只挂了附件的槽位，会直接用第一张图片附件当主视觉。
- 缩略图解码全程后台线程 + ImageIO 增量下采样（绝不整图解码），并复用全局解码限流器；加载中先显示 spinner，失败退回语义图标。

## 🔧 工程

- 新增 `RadialAttachmentPreviewPlan`（Kit 纯函数）：决定「谁上缩略图 / 谁折成 +N / 谁走小卡」，配 23 条 smoke 断言锁住「附件一个不漏、顺序稳定、计数如实」三条不变量。
- 新增扇区外弧几何 `RadialSegmentLayoutCalculator.lastPasteArc` 与编号行宽度约束 `numberRowFits`，逐扇区断言不越出楔形。
- smoke 测试总数 664 → 688，全部通过；`swift build` 无新增警告。

## 📦 安装

DMG 为 ad-hoc 签名（未走 Developer ID 公证）。若浏览器下载后被 Gatekeeper 拦截，右键「打开」或执行：

```bash
xattr -dr com.apple.quarantine /Applications/ClipSlots.app
```

SHA-256（自动更新会校验此值）：

```
b1245280c2649aedeff7591aaa3ad43bcfc5e27b8727acce16eed745fea967f3  ClipSlots_v2.11.1.dmg
```
