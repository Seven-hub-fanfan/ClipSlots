# ClipSlots v2.10.97

撤销/重做的收尾打磨：快捷键回归 macOS 标准，步数可配置且真正生效。

## 变更

### 1. 只保留 Cmd+Z（去掉 Ctrl+Z 别名）
v2.10.95/96 为照顾肌肉记忆额外注册的 `Ctrl+Z` 撤销别名已移除，撤销/重做统一为 macOS 标准键位：
- 撤销：`Cmd+Z`
- 重做：`Cmd+Shift+Z`

### 2. 新增「撤销步数」设置项（设置 → 高级）
- 默认 **10** 步，范围 **1～100**，Stepper 调节。
- 「高级」分区拆成两个小节：**日志及调试**（输出详细日志）/ **操作历史**（撤销步数）。
- 撤销步数下方实时显示**撤销栈磁盘占用**（`.undo/` 目录下 undo_stack.json + redo_stack.json）。
- 配置持久化到 `~/.config/clipslots/config.toml` 的 `undo_steps`（越界值自动夹到 1~100）。

### 3. 步数上限真正生效
- 每组撤销链上限由硬编码 10 改为**实例可配置**（`SlotUndoStack.limitPerGroup`）。
- 保存设置后：新操作按新上限截断；**已有超出部分立即截断**并落盘。
- 全局堆积上限随之等比放大（每组上限 × 3，至少 30），避免多组来回操作时无界增长。
- `config.toml` 是唯一真源：启动恢复撤销栈后按当前配置再截断一次（旧 JSON 里带的上限不算数）。
- 向后兼容：≤v2.10.96 落盘的 JSON 没有 `limitPerGroup` 字段，缺失时回落默认 10，不会因解码失败而静默丢历史。

## 验证
- `swift build` 通过。
- 零依赖 smoke 测试 **117/117 通过**（新增 UNDO-3 用例：默认值/边界夹取、自定义上限对新操作生效、调小上限立即截断、按组独立截断、全局上限放大、JSON 往返 + 老格式兼容）。
