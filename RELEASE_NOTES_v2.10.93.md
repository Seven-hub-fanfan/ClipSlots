# ClipSlots v2.10.93 — 全方位丝滑：窗口缩放 / 深浅色切换 / 切组

本轮不猜、只测：新增「单帧布局耗时」探针与「进程内窗口截图」工具，先量出瓶颈再动手，
每一处改动都有改前/改后的交替 A/B 数据，并用逐像素截图核对视觉零回归。

## 用户可感知的变化

| 交互 | v2.10.92 | v2.10.93 |
|---|---|---|
| 拖窗口边缘缩放（单次尺寸变化的同步布局，中位） | 102~113ms（≈9fps，鼠标一快就跟不上） | 27~34ms（≈3.5 倍） |
| 切深/浅色（单次主线程停顿） | 300~1600ms，>100ms 严重卡顿每次必现 | 83~93ms，**>100ms 严重卡顿 0 次** |
| 切组（累计停顿/次） | 442~497ms，严重卡顿 20~23 次 | 126~136ms，严重卡顿 0~1 次 |
| 关闭设置面板 | 158~364ms | 0~33ms |
| 打开设置面板 | 108~127ms | 76~89ms |

## 真凶与修法

**1. 窗口缩放跟不上鼠标 —— 真凶是搜索栏里的三层嵌套 `ViewThatFits`（占一帧 ≈66ms）**
`ViewThatFits` 会把**每个候选子树完整测量一遍**，嵌套后次数相乘；而这些候选里含
`.pickerStyle(.segmented)`（NSSegmentedControl）、`Menu`（NSMenu）、`TextField`（NSTextField），
**测量即实例化 AppKit 控件**。二分实测：整帧 104ms → 摘掉搜索栏 34ms → 只把嵌套 ViewThatFits
换成固定布局 38ms。改为自定义 `WidthThresholdLayout`：一次算术比较选中候选，只测量被选中的那一个。
顺带删掉从未被选中过的横向筛选 chip 条（受 400pt 限宽，任何窗口尺寸下都轮不到它）。

**2. 网格逐帧重建 —— 去掉包住整个网格的 GeometryReader（≈20ms/帧）**
原来每次尺寸变化都要重跑 GeometryReader closure、重建 10 张卡片的 View 值，并把宽度量化回写 @State。
现在只用一个 `.background` 里的极薄探针算「列数」，**只有跨列时才写一次状态**，列宽交给
`.flexible()` 在布局阶段均分。同时**删除 v2.10.75 的 resize 冻结**——A/B 证明它对帧率毫无贡献
（93.2 vs 93.5ms），只制造了「窗口边框已经走了、内容还停在上一台阶」的滞后感，即用户抱怨的「跟不上鼠标」。
现在内容与窗口边框严格同步。

**3. 深浅色「卡颜色」—— 颜色改为绘制时解析的动态色 + 只留一条主题生效路径**
- 全部颜色 token 从 `func x(_ scheme:)` 改为 `NSColor(name:dynamicProvider:)` 动态色。此前每个用色的
  视图都必须读 `@Environment(\.colorScheme)`，切一次主题就要把整棵界面连同**所有文字重新排版**
  （采样里 CoreText 占大头）。现在切主题只重绘、不重排：卡片 body 求值次数 10 → 0，
  整棵 ContentView / header / 组标签栏 / 底栏求值次数 → 0，只剩氛围层 1 次。
- 移除根视图的 `.preferredColorScheme`：主题自 v2.10.91 已由 `NSApp.appearance` 统一驱动，
  再设一次等于**两条路径各改一次 appearance = 两轮全窗重绘**，中间那一帧就是「一半旧色一半新色」。
  A/B：130ms → 87ms，严重卡顿 3/3 次 → 0 次。

## 视觉零回归的验证方式
新增进程内窗口截图（`NSView.cacheDisplay`），在深色 / 浅色 / 760pt 窄窗三档与 v2.10.92 逐像素对照：
窗口底色、卡片底色、顶部条 RGB 完全一致；网格 5 列各 513/512px、左右边距 40/41px 完全一致。
（期间发现并修正了两处自查出来的偏差：`.adaptive` 列宽会在行尾留 15pt 空隙；补偿探针若挂在已补偿的视图上会自我叠加。）

## 工程设施
- `scripts/perf_probe.sh`：一键取数（独立 bundle id + 隔离数据目录，绝不碰 `/Applications/ClipSlots.app`）。
- 新增自测场景：`resizestep`（单帧布局耗时）、`shot`（三档截图）、`themeshot`（切主题瞬间连拍）、`resize`（连续扫掠）。
- 测试：`swift run ClipSlotsKitSmokeTests` 59/59 通过。
