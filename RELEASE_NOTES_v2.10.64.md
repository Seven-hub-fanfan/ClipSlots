# ClipSlots v2.10.64

## 修复：图片存入槽位主体后，切组/切页时缩略图错误串到其它组的对应槽位

### 问题
把图片直接存进普通槽位（主体 body，而非附件）后，切换槽位组或页面时，该缩略图会错误地显示在**其它组同一序号位置**的槽位上。这是一个长期存在的显示 Bug，且只影响「主体图片」——因为主体图片的缩略图是异步解码后被持有在视图状态里的，而文本/附件预览走的是同步渲染路径。

### 根因
1. **网格单元格身份只按槽位序号标识**：`LazyVGrid` 里每个卡片用 `.id(slot)` 标识，只区分槽位序号（1..10），不区分所属页面/组。切组时 SwiftUI 认为「还是同一个 slot N 的卡片」，于是**复用**了旧组卡片视图连同它 `@State` 里已解码的 `NSImage`，导致新组的对应槽位显示上一组的缩略图。
2. **异步解码回调缺少内容级归属校验**：缩略图解码完成的回调只按弱校验落图，理论上可能写进已被复用为其它内容的单元格。

### 修复
1. **网格单元格身份改为「页面 + 组 + 槽位 + 渲染令牌」复合标识**：
   `.id(slot)` → `.id("\(currentPageId)|\(currentSpecialSlotId)|\(slot)|\(renderToken)")`。
   每个组的每个槽位都拥有独立的 SwiftUI 身份，切组时不再复用旧组卡片，`@State` 缩略图随之重建——直接根治串图。
2. **缩略图加载状态收敛为 `ThumbnailLoadState`（`@StateObject`）并加 (key, token) 双重护栏**：
   每次重载生成新 token，异步解码/QuickLook 回调/超时回调落图前都要通过 `isCurrent(key:token:)` 校验，确保迟到的解码结果绝不会写进已代表其它内容/槽位的单元格。作为纵深防御，进一步杜绝任何残留串图。

### 验证
- `swift build` 全量编译通过（仅历史无关警告）。
- `swift run ClipSlotsKitSmokeTests`：23 项 smoke 测试全绿。
