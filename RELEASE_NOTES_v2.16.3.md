# ClipSlots v2.16.3

## 画布可靠性
- 修复画布异步防抖保存的潜在竞态：所有画布文档写入改走专用串行队列，避免旧快照在极端时序下晚于新快照落盘并覆盖最新节点位置。
- 加固画布撤销/重做的槽位正文回滚：回写 handler 现在按视图实例 token 注册/清理，避免 SwiftUI 重建时旧 onDisappear 清掉新 handler；离场期间产生的回滚会排队，重新进入画布后补写。

## TapNow 画布对齐
- 轻量化历史节点连接面板的连线视觉：去掉常驻箭头与端点实心圆，hover 从红色粗线改为柔和 halo + 细线提亮。
- 拖线预览从粗蓝箭头改为更低干扰的细线/短疏虚线，命中目标时用小白点反馈，整体更接近 TapNow 的纯黑媒体画布。
- 右键操作连线时先同步选中该连线，避免上下文菜单动作和当前选中态不一致。

## 验证
- `swift build` 通过。
- `swift run ClipSlotsKitSmokeTests` 通过 32236 项，失败 0。
- DMG SHA256: `d71575ad13d7013727f033405d4b936bad777fc591efd7979adeddff12cb467e`
