# ClipSlots v2.11.6 发布说明

本版只换一样东西：**App 图标**。黑底白罐 → 白底黑石。

## 🎨 新图标

白色 squircle 底板上三块堆叠的黑色 3D 圆润形状（自下而上由大到小，像叠起来的卵石）。相比旧图标的「纯黑底板 + 扁平白色罐子」，新图标在浅色 Dock、Launchpad 和 Finder 列表里不再是一块闷黑的洞。

配套新增 `scripts/generate_app_icon_from_design.py`，从设计稿位图 `assets/AppIcon.design.png`（2048×2048）一步生成 `AppIcon.png` + `AppIcon.iconset` + `AppIcon.icns`。原有的 `scripts/generate_app_icon.sh` 走 SVG → rsvg/qlmanage → sips 的矢量路子，对位图源不适用，保留不动。

## 🔪 抠图为什么不能用亮度阈值

设计稿是黑色画布上的一块白底板，看起来「抠掉黑背景」一句话就完事。但底板上那三块图形**本身就是近黑色**（L≈30，画布背景 L≈1）。按亮度阈值抠图，会把它们连同背景一起判成透明——成品是一个空白的白色圆角方块，主体全没了。

所以 alpha 不按颜色取，按**几何形状**取：底板是一个规整的 squircle，解析地构造它的遮罩，遮罩内原样保留像素（白底板 + 黑图形），遮罩外全透明。

## 📐 squircle 参数是拟合出来的，不是猜的

把设计稿底板轮廓拟合到超椭圆 `|u|^n + |v|^n = 1`：

- 单看 RMS，**n = 5.3** 最优（误差 ≈ 边长 0.9%），也最接近 Apple 图标模板的连续曲率圆角。
- 但直接用 n=5.3 做出来的图，**左上角挂着一弯黑色月牙**。原因是 RMS 小不等于处处不越界——误差恰好集中在圆角，解析曲线在某几个角上比设计稿更饱满，于是把圆角外的纯黑画布一起框了进来。
- 补收缩量能消掉月牙，但 n=5.3 需要收缩 1.25%，整块底板跟着缩水。

最终改成**两个参数一起搜**：对每个候选 n 二分出「恰好不漏黑边」的最小收缩量，再取有效面积最大的组合。结果是 **n=4.8 + 收缩 0.05%**，覆盖设计稿底板的 98.9%——这张设计稿的圆角本来就比标准 Apple squircle 更圆一点。

顺带两个坑：

- 判断「遮罩有没有越界」需要知道底板的实心区域（含内部黑图形）。PIL 的 `ImageDraw.floodfill` 在这张图上静默失效（填充数为 0），改用「底板是凸形」这一事实：逐行取最左 / 最右白像素之间填满，逐列同理，两者取交集——对凸形精确，且不依赖 scipy。
- 设计稿底板实测 **1482×1505**，渲染稿本身就不是严格正方。图标必须正方，否则各尺寸下采样时会被 `sips` 再拉一次，差异 1.5% 虽不可见，但形变会在 16px 档暴露。生成时强制拉正。

## 🖤 补了一层投影

设计稿自带的投影落在黑画布上，抠图后必然丢失。纯白底板在 Finder 白色列表、DMG 白窗口里没有投影会「浮」得没有边界，所以重新合成了一层（下移 10、模糊 14、黑 @18%），形状直接取底板自己的 alpha，保证投影轮廓和圆角严丝合缝。

尺寸遵循 macOS 模板：1024 画布内底板 824×824 居中。那圈 100pt 留白不是浪费——Dock 放大动画、Launchpad 选中态、快速查看的投影都吃这份余量。

## ✅ 验证

图标脚本自带出厂自检（做坏的几种姿势都是「能生成、能打包、装上才发现」，所以挡在生成环节）：四角必须全透明、中心必须不透明、底板取样点必须是白的、**主体取样点必须是深色**（这条专门盯住「阈值抠图把主体一起抠掉」）、整体占位必须落在 macOS 模板量级。实测占位 850px（824 底板 + 投影外扩），全部通过。

- `swift build` 通过。
- `swift run ClipSlotsKitSmokeTests`：**通过 1134，失败 0**（与 v2.11.5 持平，本版未触碰任何逻辑代码）。
- 10 档 iconset 每一档都从 1024 母版单独 LANCZOS 下采样，不做链式缩放（链式会累积软化）。16px 下三块形状仍可辨。
- 本机覆盖安装验证：App 版本 2.11.6，`clipslots --version` 返回 2.11.6，bundle 内 `AppIcon.icns` 就位。

## 📦 校验

```text
SHA-256 (ClipSlots_v2.11.6.dmg) = 4ad542cbffc3b8525f0849ee082630301f358d4e2a0757d6d36b8841c8d8d5a5
```

DMG 为 ad-hoc 签名（`SKIP_NOTARIZE=1`），未走 Developer ID 公证；浏览器下载后如遇 Gatekeeper 提示，右键「打开」或 `xattr -dr com.apple.quarantine /Applications/ClipSlots.app` 即可。自动更新会在下载后按上表哈希校验（v2.10.67 起）。
